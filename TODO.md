- [x] 將網頁中的用語統一為「分帳」
- [x] 繼續完成各頁面的 Wireframe 文字輸出，使用 Claude Code
- [x] 繼續完成剩餘功能，使用這個 Wireframe 的資料先修正所有介面
- [x] 修正使用者設定頁面的名稱顯示問題：當未上傳大頭貼時，內部文字重疊的問題
- [ ] 修正其他地方顯示使用者頭像為文字時，文字跑版的問題
- [ ] 繼續請 Claude Code 完成剩餘的邏輯和資料串接，完成 task.md 內的其他內容
- [ ] 新增桌機版的介面設計到各個頁面
- [x] 修改各個頁面的設計：當尚未建立任何資料時（例如「我的群組」或「個人紀錄」為空白時），應顯示一個與字體顏色相同的簡潔 SVG 圖示（heroicon）來提示「尚未有任何資料」，而非直接顯示文字對於首頁來說，顯示邏輯如下：

1. 如果沒有個人紀錄，就不要顯示該區塊
2. 如果沒有群組，也不要顯示該區塊
3. 如果兩者都沒有，則顯示一個 SVG，引導使用者建立群組或是點擊記帳
- [x] 修正一些 Button 裡面的圖案表示方法：像是「全部」這個按鈕，它的右邊使用了一個「大於」符號，應該修正成使用 Heroicons 比較好
- [x] 亮色模式下「刪除」的顏色太淺，看不清楚，需要客製化
- [x] 亮色模式下的 Primary 按鈕，內部的黑色文字應該改成白色比較適合
客製化 Daisy UI 的顏色
btn-primary {
    --btn-color: var(--color-primary);
    --btn-fg: var(--color-primary-content);
}
- [x] 並且這個 primary 的綠色應該要再改深一點點，一點點就好
- [x] 右上角出現的 Toast 感覺怪怪的，這個 Toast 應該要有陰影，像彈出來的 Modal 一樣不能是扁平的，不然視覺上很突兀
- [x] Toast 的訊息必須完整一點，譬如說顯示「新增完成」，而不是紙有「完成」而已
- [x] 把建立群組頁面左上角的「取消」改成 back 圖示，並把「完成」改成 Done 的圖示
- [x] 這兩個元素不要做成一般的按鈕（Button），而是像 link 那種可點擊的型態，但一樣要保有 hover 和點擊後的視覺效果
- [x] 這樣做是為了讓圖示邊緣能對齊下方區域的元件：
   (a) 因為 form 內部會有 padding
   (b) 改成沒有邊框的按鈕樣式，才有辦法讓 back 和 Done 圖示精準對齊這個 padding，視覺顯示上才會好看
   對於其他頁面有同樣這樣的設計(例如左上角有叉叉的頁面)，也想請你參考並幫我一併修正
   希望操作的語言能夠一致：
1. 有一些是點擊後需要按「取消」
2. 有一些是顯示「X」
3. 有一些是顯示「Back」

請你判斷，應該大部分都可以直接改成「Back」就好。
- [x] 點擊「Back」之後，會有這個 Dialog 跳出來，希望改成使用這個主題的這一種 Modal，而不是這種瀏覽器的 Dialog，這樣子視覺上會更加一致。
- [x] 讓使用者可以停留在群組頁面的設計，也就是讓群組頁面直接當成 App 的主頁。

這樣使用者如果在旅遊當中，就不需要一直重新點擊並進入群組才能記帳，而是一直待在該群組內，直到點擊 Navigation Bar 離開群組頁面為止。

這部分需要檢查現在是否已經完成了。

- [x] 移除中間這一個記帳的按鈕，因為這個設計太模稜兩可了，使用者會不知道到底是要記到群組還是記到個人。

所以使用者操作前，都需要確保他現在的 Tab 在哪裡：
1. 如果在群組的地方記，就是記到群組
2. 在個人的頁面記，就是記到個人

當然，個人記帳的朋友下拉式選單是共用於群組的資訊，這部分還是要依照之前的需求，按照頻率排序。

修改需求書：將「最佳分帳」按鈕的功能先註解掉，並註記「暫時不開發」。

這主要有兩個原因：
1. 產品設計：目前這項功能在產品設計上會產生一些問題，使用者選擇「開啟」或「不開啟」，在產品設計的操作方法上會很不一樣，導致不容易設計。
2. 使用者體驗：不確定使用者是否真的希望以這種方式操作。讓朋友們互相去解決債務問題，可能會讓他們感覺不夠直接。

- [x] 並且幫我移除現在頁面上「最佳分帳」的這個功能

- [x] 按鈕設計：
   Modal 當中「放棄」按鈕的文字顏色若用黑色會看不清楚，這邊應該要用白色的字。

- [x] 設定頁面：
語言上方多出了一條分隔線，這個部分想要移除

- [x] 群組內頁設計：
   希望在這個群組的內頁，將卡片的設計也換成像外面這種使用分隔線（分隔符）的設計。建議參考首頁的樣式，而不是使用卡片，因為卡片在手機螢幕上的顯示效果會非常有限。

總花費的部分也想改成像是首頁的這種設計。這部分想請你幫忙看看，如何在改動後仍能保持視覺上的協調，不要有違和感。

- [x] 修改群組內頁的排版：

1. 新增一個名為「設定」的 Tab
   (a) 將「編輯群組資料」功能移入此 Tab
   (b) 將「刪除群組」功能移入此 Tab

2. 調整右上角按鈕
   (a) 將原本「三個點」的更多選單按鈕，更換為「分享」的 icon
   (b) 點擊後串接手機系統自動彈出的分享選單
   (c) 支援分享連結至 Facebook、Messenger、Instagram 或 LINE 等平臺

3. 製作群組分帳總覽：
   (a) 點擊「代墊項目」後，應導向詳情頁面
   (b) 詳情頁面需顯示金額拆分的細節
   (c) 若有上傳收據等附件，也應一併顯示
   (d) 並具備編輯功能

- [x] Header 位置修正
   目前群組內頁上方會顯示一張首圖，但返回鍵（back）和上方的 Header 卻是貼在圖片的下邊緣，而非最頂端。這部分需要修正，將它們移回最上方。

- [x] 移除群組內頁總覽顯示圖片：
   直接移除在群組內頁總覽這邊顯示的圖片。可以改到「設定」的 Tab 去顯示，在設定的頁面顯示這個圖就好。 

- [x] 重新設計總覽頁面上半部：
``` wireframe
[ 導覽列：群組名稱 ⚙️ ]
-------------------------------------------------
(頂部區塊：成員待付總額，預設顯示前三高)

  陳思霓
  待付 NT$ 1,500
  █████████████░░░░░░░░░░░░░░░░░░░░░

  宇軒
  待付 NT$ 1,200
  ██████████░░░░░░░░░░░░░░░░░░░░░░░░

  佳穎
  待付 NT$ 800
  ██████░░░░░░░░░░░░░░░░░░░░░░░░░░░░

  [ 🔽 展開顯示所有成員 ]

-------------------------------------------------
(底部區塊：詳細記錄，佔據剩餘螢幕空間)

📝 詳細記錄                             [依日期 🔽]

🛒 晚餐 (水餃)        |  宇軒墊付    |  $ 600
🎲 大地遊戲道具      |  佳穎墊付    |  $ 350
🚖 計程車           |  宇軒墊付    |  $ 200
🍺 梅啤             |  永辰墊付    |  $ 600
(向下無限滾動...)
```
<progress className="progress progress-error w-56" value={0} max="100"></progress>
<progress className="progress progress-error w-56" value="10" max="100"></progress>
<progress className="progress progress-success w-56" value={0} max="100"></progress>
<progress className="progress progress-success w-56" value="10" max="100"></progress>


- [ ] 將瀏覽器 Dialog 替換為 Modal Popup：
   現在有一些功能的確認視窗，還是使用瀏覽器的 Dialog 而不是 Modal 的 Popup（例如「一鍵全部標記完成」這個功能）。請幫我找出所有這類型的視窗，並統一替換成 Modal。

- [ ] 修正使用者頭像顯示：
   (a) 如果使用者沒有設定圖片，頭像會顯示一個字。目前字體位置偏左，請協助將其修正為置中。
   (b) 重新設計頭像底色：目前的「總花費」區域是黑色底，但下方的「記帳區」卻是淺灰色底，視覺上不一致。請協助重新設計顏色，或是考慮使用細微的漸層（但要注意不要太花俏），看看怎樣的設計會更好。

- [ ] 修正群組成員與動態：
   在「群組內成員與動態」區域，目前使用者的頭像沒有正常顯示出來，這部分也請協助檢查並修正。

- [ ] 調整 Toast 顯示位置：
   Toast 的顯示位置想要改在左下角，而不是右上角。因為右上角在手機介面上時常有操作項，容易造成幹擾，所以想改到左下。

   但請注意，因為首頁有 Navigation Bar，所以它的位置應該要在 Navigation Bar 的上面一點點，而不會遮擋到導航欄,而在沒有導航欄的頁面，位置應該要調整得比較下方一點。

- [ ] 調整個人記帳的操作流程：
   現在是要先點擊新增朋友的名字，我想請你幫我改成：在個人頁面一旦點擊新增，就是直接新增這筆分帳，不用先選擇使用者。

   使用者的選取或輸入，應該是在記帳頁面裡面進行，這樣設計比較直觀。
   
   所以希望把個人頁面的「新增」按鈕，移到右下角這種懸浮的圓形，和群組內部或個人內部的按鈕一致，去統一這個操作。

   它只需要點擊這一個就可以記帳，當然在個人頁面裡面，使用者還是可以點擊這個「＋」去記帳。


- [ ] 修正群組內頁的付款人下拉式選單與分帳對象的 Checkbox List：
   為什麼現在這個群組裡面，在「新增帳務」時，付款人的下拉式選單會跳不出來？
   然後，分帳對象的 Checkbox List（勾選清單）也出不來。這個部分想請你幫忙修正。
在於「個人分帳」應該也有這一個問題，想請你幫忙修改。

- [ ] 修正一些頁面的按鈕設計不一致的問題：
   譬如說建立群組（Groups News）的頁面，它的 Back 鍵和打勾鍵是圓角正方形，但其他頁面卻是圓形的按鈕。這個部分感覺有點怪怪的，想請你幫忙修正一下。全部請你幫我統一成圓形。


- [ ] 幫我修改現在的一些用語和文案。
「欠」這個字確實在心理學上帶有「負債、道德壓力」的貶義，如果用在好朋友之間，有時會讓人覺得稍微有些銳利或尷尬，這也與 Zplit 想強調的「長久相伴、無縫接軌」的友善氛圍背道而馳。

要把文案變得優雅且容易理解，我們可以根據不同的介面情境，將「負面的狀態」轉化為「中性的動作」或「友善的日常對話」。以下提供三個方向的替代用語：

### 1. 著重「動作」與「金錢流動」（最推薦，直覺且專業）
將焦點從「誰負債」轉移到「接下來要發生什麼動作」，這能大幅降低金錢往來的壓力感。
* **別人欠你** $\rightarrow$ **將收取** / **應收**
* **你欠別人** $\rightarrow$ **需支付** / **應付**
* **UI 呈現範例**：「你將從 宇軒 收取 NT$500」、「你需支付 佳穎 NT$200」。

### 3. 簡潔的會計/儀表板風格（最俐落，適合總覽頁面）
在「Tab 1：總覽（Summary）」這類需要一眼看清楚多筆數字的畫面，建議用最精簡的詞彙，確保版面乾淨。
* **別人欠你** $\rightarrow$ **待收款** / **待收**
* **你欠別人** $\rightarrow$ **待付款** / **待付**
* **UI 呈現範例**：在頂部卡片顯示「總待收 NT$1,000」與「總待付 NT$300」，下方列表則寫「待付給 佳穎 NT$200」。

---

**設計小建議：**
你可以將**方案一**用在具體的單筆操作或「債務清算」列表中，讓指令明確；而將**方案三**用在首頁的數字總覽上，保持介面的專業與整潔。

- [ ] 在設定頁面上，這個語言和主題的設定區塊感覺怪怪的。

主要問題如下：
1. 綠色太顯眼了。
2. 按鈕的設計讓它不太像是在做列表選單。

所以想請你幫我改成使用 Daisy UI 裡面的 Pagination 做法，等一下這邊我會附上程式碼。

<div className="join">
  <button className="join-item btn">1</button>
  <button className="join-item btn btn-active">2</button>
  <button className="join-item btn">3</button>
  <button className="join-item btn">4</button>
</div> 但是這部分要注意，要讓這個按鈕 join 的長度填滿這個容器的寬，這樣視覺上才會比較平衡。


- [ ] 想要將個人分帳的新增分帳頁面，儲存按鈕也改到右上角變成勾勾，一致化這個設計
- [ ] 個人分帳的新增分帳頁面上有顯示「你替 Contact 付款」和「Contact 代墊」，這個 Contact 可以幫我改成他的名字嗎？不要直接使用 Contact

- [ ] 想請你幫忙將現在的各頁面的 Loading 動畫，改成 Daisy UI 內建的 Skeleton
<div className="flex w-52 flex-col gap-4">
  <div className="flex items-center gap-4">
    <div className="skeleton h-16 w-16 shrink-0 rounded-full"></div>
    <div className="flex flex-col gap-4">
      <div className="skeleton h-4 w-20"></div>
      <div className="skeleton h-4 w-28"></div>
    </div>
  </div>
  <div className="skeleton h-32 w-full"></div>
</div>

- [ ] 幫我修改群組頁面，將右上角的這個「新增」按鈕改成「新增群組」，這樣比較明確。

- [ ] 請幫忙修改一下。現在所有頁面上都有使用這個分隔線（divider）的設計，因為在手機畫面上使用卡片效果不太好，但這個分隔線目前的長度都會直接與畫面邊緣連在一起。

想請你幫忙調整，讓它不要連到邊緣，而是限制在容器內部，不要擴展到 padding 區域。現在它連 padding 的地方也會顯示這條線，感覺畫面會有一種被截斷的錯覺，但其實它們是同一個區塊。所以想請你針對這個部分進行修改。

- [ ] 對於出現在個人紀錄和其詳細內頁裡面，都會有顯示「別人欠多少」或「我們需要支付別人多少」的區塊。

這部分你可以幫我改成使用這種的元件嗎？

<div className="stats bg-base-100 border-base-300 border">
  <div className="stat">
    <div className="stat-title">Account balance</div>
    <div className="stat-value">$89,400</div>
    <div className="stat-actions">
      <button className="btn btn-xs btn-success">Add funds</button>
    </div>
  </div>

  <div className="stat">
    <div className="stat-title">Current balance</div>
    <div className="stat-value">$89,400</div>
    <div className="stat-actions">
      <button className="btn btn-xs">Withdrawal</button>
      <button className="btn btn-xs">Deposit</button>
    </div>
  </div>
</div>

- [ ] 目前群組「成員與動態」這邊，已綁定的使用者會出現「已連結」的 Badge。

這個 Badge 的配色仍然需要修正，請幫我改成 Soft Style，謝謝。

<div className="badge badge-soft badge-primary">Primary</div>
<div className="badge badge-soft badge-secondary">Secondary</div>
<div className="badge badge-soft badge-accent">Accent</div>
<div className="badge badge-soft badge-info">Info</div>
<div className="badge badge-soft badge-success">Success</div>
<div className="badge badge-soft badge-warning">Warning</div>
<div className="badge badge-soft badge-error">Error</div>

一般的
<span className="badge">Badge</span>

- [ ] 修正記帳頁面自動設定的日期與時間，時區好像不在臺灣。
群組的總覽頁面，在「代付」的清單裡面也需要顯示「代收」，並顯示在最後面。

關於代收的部分：
1. 顯示位置：清單最後面
2. 樣式設定：使用綠色的 Progress Bar

- [ ] 在「新增帳務」的頁面，付款人與分帳對象的資料在進入頁面時載入不出來，但重新整理後就會出現，這部分需要修正。

是不是可以將這個群組資料快取到本地呢？這樣就不用每次都從資料庫拿取。

- [ ] 在顯示使用者名稱的時候，不用顯示為「建立者」。
就是在這個「新增帳務」的頁面，付款人這邊都會顯示建立者，這部分不需要顯示。

- [ ] 所有上傳圖片的區塊都要注意：上傳之後，不能根據圖片的寬度就將該區塊的寬度改變，而是要維持原本的區塊大小，並將圖片填滿在區塊內顯示。

- [ ] 在「平均分帳」的模式下，新增帳務時，是否可以將一列（Row）顯示兩個欄位（Column），讓兩個使用者同時勾選？
不然現在所有使用者的排版都擠在左邊，視覺上比較不平衡。

- [ ] 想請你幫忙修改群組內頁的 Header。目前在名稱下面會顯示「有幾位成員」，想請你幫我移到群組名稱的右邊，而不是顯示在下面。
因為顯示在下面會導致 Header 高度增加，使得這個按鈕的位置和其他頁面的按鈕位置不一致，所以想請你幫我將「幾位成員」的文字移到群組名稱的右邊。

- [ ] 修正群組詳細記錄的項目，解決點擊後無法進入頁面修改的問題。這修改頁面其實與「新增帳務」的頁面非常相似。

- [ ] 現在這個清算的頁面，有幾點想請你優化：

1. 轉帳用詞修改
   請將「轉帳給某某」的用詞，統一改為「支付給某某」

2. 按鈕 UI 重新設計
   「標記完成」的按鈕現在看起來有點醜，不知道是因為顯示在第二個 row 還是其他原因，感覺需要重新設計一下，看要如何呈現「標記為完成」的操作

- [ ] 總覽詳細記錄介面優化
   在「總覽詳細記錄」這邊，原本每個項目裡面都會有 Group Avatar，顯示這位使用者是幫誰代墊的。現在這個 Group Avatar 組件被移除了，想請你幫我新增回來，謝謝。